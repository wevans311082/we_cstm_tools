# va-workspace — implementation pointer

This file is **not** the product spec. Do not implement from the old six-phase codegen prompt.

**Source of truth:** [`SPEC.md`](SPEC.md)

- CLI: `va`
- Package: `va_workspace`
- Runtime: Kali Linux (pipx + apt/pipx binaries)
- Dev: Windows-friendly parser, vault, ingest, and unit tests (no live Nmap required)
- Findings are operator-authored; tools produce evidence and leads only
- No placeholders, strict type hints, no `shell=True`
- Authorised-use only. Not NCSC-endorsed.

Implement the next unfinished PR in SPEC.md §13.
